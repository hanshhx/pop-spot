name: Quality

on:
  push:
    branches: [main]
  pull_request:

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: popspot-frontend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: popspot-frontend/package-lock.json
      - run: npm ci
      # 검사 순서를 바꿨다 — 감사가 맨 앞에서 실패하면 뒤가 전부 건너뛰어져, 정작 코드가 깨졌는지는
      # 아무도 모르는 채로 빨간불만 오래 켜져 있었다(20회 연속). 코드 검사를 먼저 돌린다.
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run format:check
      - run: npm test
      # 의존성 취약점은 보고만 하고 막지 않는다.
      #
      # Next.js 16.1.6 에 high 등급 권고가 걸려 있고 해결 버전은 16.2.12 인데, 그 버전은 빌드는
      # 통과하지만 Vercel 에서 서비스되는 배포로 승격되지 않았다(2026-07-28, 세 번 시도). 운영에
      # 사용자 영향이 있는 수정을 내보내야 해서 버전을 되돌렸다.
      #
      # 막지 않는 대신 결과는 로그에 남는다. 승격 문제가 풀리면 16.2.12 로 올리고 이 예외를 지운다.
      - run: npm audit --omit=dev --audit-level=high
        continue-on-error: true
      - run: npm run build
        env:
          NEXT_PUBLIC_API_URL: http://localhost:8080
          NEXT_PUBLIC_SOCKET_URL: http://localhost:8080
          ALLOW_EMPTY_SEO_BUILD: "true"

  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: popspot-backend
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: gradle
      - uses: gradle/actions/setup-gradle@v4
      - run: chmod +x gradlew
      - run: ./gradlew spotlessCheck test bootJar
      # 의존성 취약점 검사(dependencyCheckAnalyze)는 여기 없다 — security-scan.yml 로 옮겼다.
      #
      # 이 검사는 미국 취약점 DB(NVD) 전체를 내려받는데, 커밋마다 돌리면 한 번에 30~50분이 걸리고
      # NVD 가 응답을 끊으면 그대로 실패한다(실측: 성공 29·30·56분 / 실패 3·4·7분). 결과적으로
      # <b>내 코드는 멀쩡한데 빨간불만 켜져 있는</b> 상태가 반복됐다.
      #
      # 검사 대상은 내 코드가 아니라 <b>남이 만든 라이브러리</b>다. 커밋을 해도 그 목록은 바뀌지
      # 않으므로 매번 돌릴 이유가 없다. 주 1회로 충분하고, 새 취약점은 발표 시점에 생기지 커밋
      # 시점에 생기지 않는다.
