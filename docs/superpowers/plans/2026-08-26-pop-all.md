# POP-ALL — 미리보기 + 전체보기 대형 모달 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 홈 벤토의 「N곳으로 들어가는 문」(랜딩 링크 4개)을 **POP-ALL** 로 바꾼다 — 카테고리별 가로 스크롤 미리보기 + 수백 곳을 실제로 훑을 수 있는 대형 모달(검색·필터·정렬·페이지네이션·최근 본 팝업·이미 본 곳 표시·배지·빈 결과 안내·키보드·애니메이션).

**Architecture:** 홈은 이미 `apiFetch('/api/popups')` 로 **전체 목록을 클라이언트 메모리에 들고 있다**(`HomeClient.tsx:1106`, `allPopups` 실측 1,002곳). 따라서 검색·필터·정렬·페이지네이션은 **네트워크 호출 0번**, 순수 배열 연산이다. 계산은 전부 `src/lib/` 의 순수 함수로 빼서 node 환경 vitest 로 검증하고, 컴포넌트는 그 결과를 그리기만 한다. 모달 껍데기는 이미 있는 `DialogContent size="full"`(`max-w-[95vw] h-[90vh]`)을 쓴다.

**Tech Stack:** Next.js App Router, React 19, TypeScript strict, Tailwind v4, Radix Dialog, framer-motion 12, vitest 4 (node env, `globals: false`)

## Global Constraints

- **인프라 비용 0원.** 새 API·새 백엔드 호출을 만들지 않는다. 모든 계산은 이미 받은 `allPopups` 위에서 한다.
- **백엔드 변경 금지.** 이 계획은 프론트 전용이다(백엔드는 수동 jar 배포라 프론트 배포에 못 딸려간다).
- **`npm run format` 금지.** 파일별로 `npx prettier --write <파일>` 만 쓴다.
- **`npm run format:check` 는 이미 빨간불**(`src/data/emergency/popups-2026-08-11.json`) — 기존 문제이므로 무시한다.
- **테스트는 node 환경, `globals: false`** — 모든 테스트 파일 맨 위에 `import { describe, expect, it } from 'vitest';` 를 명시한다. DOM 이 필요하면 파일 상단에 `// @vitest-environment jsdom` 을 단다.
- **i18n 키는 ko/en/ja 셋 다** 채운다. `src/lib/i18n.tsx:2406` 의 `_localeParity` 가 하나라도 빠지면 `tsc` 를 깨뜨린다 — 이게 안전망이다.
- **날짜 로직에 `src/lib/dday.ts` 를 쓰지 않는다.** 로컬 `setHours` 가 Vercel UTC 에서 KST 00:00–09:00 사이 하루를 밀어낸다. `kstTodayStart()` / `landingStatus()` / `parseDate()` / `startOfDay()` 를 쓴다.
- **주석은 한국어**, `<b>`/`<p>` 를 써서 *왜* 그렇게 했는지 적는다. 테스트의 `it()` 문자열도 한국어 평서문 한 문장.
- Prettier: singleQuote, semi, printWidth 100, trailingComma "all", arrowParens "always", endOfLine **lf**.
- **`/planning`(작전지도)은 폐기됐다.** 어떤 것도 여기 연결하지 않는다.
- **랜딩 페이지(`/popups/[slug]`)로 보내지 않는다.** 이 계획의 존재 이유가 그 재탕을 없애는 것이다.

## 숫자에 대한 결정 (읽고 시작할 것)

홈이 말하는 "850곳" = `mappablePopups.length` (`HomeClient.tsx:530`) — **중복 제거 전** 값이다. 한편 같은 행사가 이름만 다른 여러 줄로 들어오는 문제 때문에 `dedupedPopups`(`HomeClient.tsx:435`, `groupSameEvent` + viewCount 합산)가 따로 있다.

**POP-ALL 은 `mappablePopups` 를 `groupSameEvent` 로 묶은 것을 유일한 목록으로 쓴다.** 이유:

- 묶지 않으면 "스트릿 레스토랑 파이터" 가 4줄로 나와 한 페이지(24칸) 중 4칸을 한 행사가 먹는다. 몇백 곳을 다양하게 보여주는 것이 이 화면의 목적인데 정반대가 된다.
- 850(안 묶음)과 목록 길이(묶음)가 다르면 **한 화면이 "전체" 를 두 숫자로 말하게 된다** — 이 코드베이스가 이미 한 번 고친 버그다(`HomeClient.tsx:527` 주석).

**따라서 `mappablePopupCount` 자체를 묶은 값으로 바꾼다**(Task 8). 화면에 보이는 숫자가 850에서 조금 내려간다. 이건 의도된 변화이고, "셀 수 있는 것이 아니라 갈 수 있는 것을 센다" 는 기존 원칙의 연장이다 — 같은 행사를 네 번 세는 것은 갈 수 있는 곳을 네 곳으로 세는 것이다.

## 태스크 순서에 대한 주의

Task 4(카드)는 Task 5(i18n)가 만드는 `popall.badgeDday` 등을 쓴다. 그래서 **번호 순서대로 하면 Task 4 에서
컴파일이 깨진다.** 실행 순서는 **1 → 2 → 3 → 5 → 4 → 6 → 7 → 8 → 9** 로 한다.

## File Structure

**만드는 파일**

| 파일 | 책임 |
|---|---|
| `src/lib/popupBadges.ts` | 팝업 하나 → 배지 하나(오늘 오픈 / D-N 마감 / 오픈 예정). 순수 |
| `src/lib/popupBadges.test.ts` | 위 테스트 |
| `src/lib/popAllPreview.ts` | 미리보기용 카테고리별 줄 계산(사진 있는 것만, 날짜 시드 회전). 순수 |
| `src/lib/popAllPreview.test.ts` | 위 테스트 |
| `src/lib/popAllQuery.ts` | 모달의 검색·필터·정렬·페이지네이션 + 빈 결과 완화 제안. 순수 |
| `src/lib/popAllQuery.test.ts` | 위 테스트 |
| `src/components/main/PopAllPreview.tsx` | 벤토 히어로 자리의 미리보기 타일 UI |
| `src/features/popup/PopAllModal.tsx` | 대형 모달 껍데기 + 그리드 + 페이지네이션 |
| `src/features/popup/PopAllFilterBar.tsx` | 검색창 + 지역/카테고리/배지 필터 + 정렬 |
| `src/features/popup/PopAllRecentPanel.tsx` | 오른쪽 「최근 본 팝업」 패널 |
| `src/features/popup/usePopAllKeyboard.ts` | `/` 포커스, `Esc` 초기화, `←/→` 페이지 |
| `src/features/popup/usePopAllKeyboard.test.tsx` | 위 테스트(jsdom) |

**고치는 파일**

| 파일 | 무엇을 |
|---|---|
| `src/components/main/PopupCard.tsx` | 선택적 `seen?: boolean`, `badge?: ReactNode` 추가 |
| `src/components/main/HomeBento1a.tsx` | 첫 타일(문 4개)을 `PopAllPreview` 로 교체. 여권·일정 타일은 그대로 |
| `app/HomeClient.tsx` | `catalogDoors` 제거, POP-ALL 배선, `mappablePopupCount` 를 묶은 값으로 |
| `src/lib/i18n.tsx` | 새 키를 ko/en/ja 에 추가 |

**지우는 파일**

- `src/lib/catalogDoors.ts`, `src/lib/catalogDoors.test.ts` — 랜딩으로 보내는 문 계산. 이 계획이 대체한다.

---

### Task 1: 배지 판정 (오늘 오픈 / 마감 D-N)

**Files:**
- Create: `popspot-frontend/src/lib/popupBadges.ts`
- Test: `popspot-frontend/src/lib/popupBadges.test.ts`

**Interfaces:**
- Consumes: `landingStatus`, `LandingStatus` (`src/lib/landingStatus.ts`)
- Produces: `export type PopupBadge`, `export function popupBadge(startDate, endDate, today): PopupBadge`, `export const CLOSING_SOON_DAYS = 3`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`popspot-frontend/src/lib/popupBadges.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { popupBadge } from './popupBadges';

// kstTodayStart() 가 돌려주는 것과 같은 모양 — '로컬 자정으로 표현한 달력 날짜'.
const TODAY = new Date(2026, 7, 26);

describe('popupBadge', () => {
  it('오늘 문을 연 팝업에는 오늘 오픈 배지를 준다', () => {
    expect(popupBadge('2026-08-26', '2026-09-30', TODAY)).toEqual({ kind: 'openingToday' });
  });

  it('사흘 안에 끝나는 팝업에는 남은 날짜와 함께 마감 배지를 준다', () => {
    expect(popupBadge('2026-08-01', '2026-08-29', TODAY)).toEqual({
      kind: 'closingSoon',
      dday: 3,
    });
  });

  it('오늘 끝나는 팝업의 남은 날짜는 0이다', () => {
    expect(popupBadge('2026-08-01', '2026-08-26', TODAY)).toEqual({
      kind: 'closingSoon',
      dday: 0,
    });
  });

  it('오늘 열고 사흘 안에 닫는 팝업은 오늘 오픈이 아니라 마감 배지를 단다', () => {
    // 둘 다 해당할 때 무엇을 보여줄지는 취향이 아니라 행동의 문제다 — 마감이 사람의
    // 계획을 바꾸고, 오늘 열었다는 사실은 바꾸지 않는다.
    expect(popupBadge('2026-08-26', '2026-08-27', TODAY)).toEqual({
      kind: 'closingSoon',
      dday: 1,
    });
  });

  it('아직 열지 않은 팝업에는 며칠 뒤에 여는지를 준다', () => {
    expect(popupBadge('2026-08-30', '2026-09-30', TODAY)).toEqual({
      kind: 'upcoming',
      opensIn: 4,
    });
  });

  it('한참 남은 팝업에는 배지를 주지 않는다', () => {
    expect(popupBadge('2026-08-01', '2026-12-31', TODAY)).toBeNull();
  });

  it('이미 끝난 팝업에는 배지를 주지 않는다', () => {
    expect(popupBadge('2026-07-01', '2026-08-25', TODAY)).toBeNull();
  });

  it('종료일을 모르는 상시 운영 팝업에는 배지를 주지 않는다', () => {
    // 날짜를 모르는 것은 급하다는 뜻이 아니다. 모를 때 급한 척하는 배지가 가장 나쁘다.
    expect(popupBadge('2026-08-01', null, TODAY)).toBeNull();
  });

  it('시작일을 모르면 오늘 오픈이라고 주장하지 않는다', () => {
    expect(popupBadge(null, '2026-12-31', TODAY)).toBeNull();
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd popspot-frontend && npx vitest run src/lib/popupBadges.test.ts
```

Expected: FAIL — `Failed to resolve import "./popupBadges"`

- [ ] **Step 3: 최소 구현을 쓴다**

`popspot-frontend/src/lib/popupBadges.ts`:

```ts
import { landingStatus } from './landingStatus';
import { parseDate, startOfDay } from './popupSlices';

/** 마감 배지를 다는 기준 — 며칠 남았을 때부터 급한가. */
export const CLOSING_SOON_DAYS = 3;

/**
 * 목록의 한 칸이 달 수 있는 배지. 없으면 null.
 *
 * <p>배지는 <b>한 팝업에 하나만</b> 붙는다. 두 개가 붙으면 칸이 좁아지는 것보다 나쁜 일이
 * 생긴다 — 무엇을 먼저 읽어야 할지가 사라진다.
 */
export type PopupBadge =
  | { kind: 'openingToday' }
  | { kind: 'closingSoon'; dday: number }
  | { kind: 'upcoming'; opensIn: number }
  | null;

/** 오늘로부터 며칠 뒤인가. 읽을 수 없으면 null. */
function daysUntil(value: string | null | undefined, today: Date): number | null {
  const parsed = parseDate(value ?? null);
  if (!parsed) return null;
  return Math.round((startOfDay(parsed).getTime() - today.getTime()) / 86_400_000);
}

/**
 * 팝업 하나가 달 배지를 고른다.
 *
 * <p><b>우선순위는 마감이 먼저다.</b> 오늘 열고 내일 닫는 팝업이 있을 때 「오늘 오픈」을
 * 보여주면 사실이긴 해도 사람을 헛걸음시킨다. 배지가 하는 일은 소식 전달이 아니라
 * <b>계획을 바꾸는 것</b>이고, 계획을 바꾸는 쪽은 언제나 마감이다.
 *
 * <p>날짜를 모르면 배지를 달지 않는다. 이 목록은 이미 만료된 것을 걸러낸 뒤라 날짜 미상은
 * 상시 운영에 가깝다 — 모르는 것을 급한 것으로 바꿔 말하지 않는다.
 */
export function popupBadge(
  startDate: string | null | undefined,
  endDate: string | null | undefined,
  today: Date,
): PopupBadge {
  const status = landingStatus(startDate ?? null, endDate ?? null, today);
  if (status.kind === 'ended') return null;
  if (status.kind === 'upcoming') return { kind: 'upcoming', opensIn: status.opensIn };

  if (status.dday !== null && status.dday <= CLOSING_SOON_DAYS) {
    return { kind: 'closingSoon', dday: status.dday };
  }
  if (daysUntil(startDate, today) === 0) return { kind: 'openingToday' };
  return null;
}
```

- [ ] **Step 4: 통과를 확인한다**

```bash
cd popspot-frontend && npx vitest run src/lib/popupBadges.test.ts && npx tsc --noEmit -p tsconfig.json
```

Expected: 9 tests PASS, tsc 무출력

- [ ] **Step 5: 커밋**

```bash
npx prettier --write popspot-frontend/src/lib/popupBadges.ts popspot-frontend/src/lib/popupBadges.test.ts
git add popspot-frontend/src/lib/popupBadges.ts popspot-frontend/src/lib/popupBadges.test.ts
git commit -m "feat(popall): 오늘 오픈·마감 임박 배지를 한 곳에서 판정한다"
```

---

### Task 2: 미리보기 줄 계산 (카테고리별 10곳, 매일 회전)

**Files:**
- Create: `popspot-frontend/src/lib/popAllPreview.ts`
- Test: `popspot-frontend/src/lib/popAllPreview.test.ts`

**Interfaces:**
- Consumes: `classifyCategory`, `CategoryCode`, `CATEGORIES` (`src/lib/popupSlices.ts`), `popupCoverUrl` (`src/lib/popupCover.ts`), `PopupStore` (`src/types/popup.ts`)
- Produces: `export interface PreviewRow { code: CategoryCode; popups: PopupStore[] }`, `export function popAllPreviewRows(popups, today, options?): PreviewRow[]`, `export const PREVIEW_ROW_COUNT = 4`, `export const PREVIEW_PER_ROW = 10`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`popspot-frontend/src/lib/popAllPreview.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { popAllPreviewRows } from './popAllPreview';
import type { PopupStore } from '@/types/popup';

const TODAY = new Date(2026, 7, 26);
const TOMORROW = new Date(2026, 7, 27);

/** 사진이 있는 팝업 하나. photoOrigin=CRAWLED 라야 popupCoverUrl 이 URL 을 돌려준다. */
function popup(id: number, category: string, overrides: Partial<PopupStore> = {}): PopupStore {
  return {
    id,
    name: '팝업 ' + id,
    location: '성수동',
    status: '',
    viewCount: 0,
    category,
    imageUrl: 'https://cdn.example.com/' + id + '.jpg',
    photoOrigin: 'CRAWLED',
    ...overrides,
  } as PopupStore;
}

/** 같은 카테고리 팝업을 n개. id 는 base+1 부터. */
function many(base: number, category: string, n: number): PopupStore[] {
  return Array.from({ length: n }, (_, i) => popup(base + i + 1, category));
}

describe('popAllPreviewRows', () => {
  it('카테고리마다 한 줄씩 만든다', () => {
    const rows = popAllPreviewRows([...many(0, '패션', 12), ...many(100, '뷰티', 12)], TODAY);
    expect(rows.map((r) => r.code).sort()).toEqual(['beauty', 'fashion']);
  });

  it('한 줄에는 열 곳을 담는다', () => {
    const rows = popAllPreviewRows(many(0, '패션', 30), TODAY);
    expect(rows[0].popups).toHaveLength(10);
  });

  it('열 곳이 안 되는 카테고리는 있는 만큼만 담는다', () => {
    const rows = popAllPreviewRows(many(0, '패션', 6), TODAY);
    expect(rows[0].popups).toHaveLength(6);
  });

  it('사진이 없는 팝업은 미리보기에 넣지 않는다', () => {
    // 미리보기의 약속이 "사진과 제목" 이므로, 사진이 없으면 그 약속을 지킬 수 없다.
    const withPhoto = many(0, '패션', 5);
    const noPhoto = many(100, '패션', 5).map((p) => ({ ...p, imageUrl: undefined }));
    const rows = popAllPreviewRows([...withPhoto, ...noPhoto], TODAY);
    expect(rows[0].popups.map((p) => p.id).sort((a, b) => a - b)).toEqual([1, 2, 3, 4, 5]);
  });

  it('팝업이 세 곳도 안 되는 카테고리는 줄을 만들지 않는다', () => {
    // 두 칸짜리 가로 스크롤 줄은 스크롤할 것이 없어 줄로서 실패한다.
    const rows = popAllPreviewRows([...many(0, '패션', 12), ...many(100, '뷰티', 2)], TODAY);
    expect(rows.map((r) => r.code)).toEqual(['fashion']);
  });

  it('분류되지 않는 카테고리는 줄을 만들지 않는다', () => {
    // 줄 제목을 지을 수 없는 축은 줄이 될 수 없다.
    const rows = popAllPreviewRows([...many(0, '패션', 12), ...many(100, '어쩌구', 12)], TODAY);
    expect(rows.map((r) => r.code)).toEqual(['fashion']);
  });

  it('기본으로 네 줄까지만 만든다', () => {
    const rows = popAllPreviewRows(
      [
        ...many(0, '패션', 12),
        ...many(100, '뷰티', 12),
        ...many(200, '캐릭터', 12),
        ...many(300, '디저트', 12),
        ...many(400, '아트', 12),
      ],
      TODAY,
    );
    expect(rows).toHaveLength(4);
  });

  it('같은 날 두 번 불러도 같은 결과를 준다', () => {
    const pool = [...many(0, '패션', 30), ...many(100, '뷰티', 30)];
    expect(popAllPreviewRows(pool, TODAY)).toEqual(popAllPreviewRows(pool, TODAY));
  });

  it('하루가 지나면 줄 안의 팝업이 달라진다', () => {
    // 매일 같은 열 곳만 보여주면 재방문자에게 이 자리는 두 번째부터 죽은 칸이 된다.
    const pool = many(0, '패션', 30);
    const before = popAllPreviewRows(pool, TODAY)[0].popups.map((p) => p.id);
    const after = popAllPreviewRows(pool, TOMORROW)[0].popups.map((p) => p.id);
    expect(after).not.toEqual(before);
  });

  it('하루가 지나면 보여주는 카테고리 조합도 달라진다', () => {
    const pool = [
      ...many(0, '패션', 12),
      ...many(100, '뷰티', 12),
      ...many(200, '캐릭터', 12),
      ...many(300, '디저트', 12),
      ...many(400, '아트', 12),
    ];
    const before = popAllPreviewRows(pool, TODAY).map((r) => r.code);
    const after = popAllPreviewRows(pool, TOMORROW).map((r) => r.code);
    expect(after).not.toEqual(before);
  });

  it('빈 목록에는 줄을 만들지 않는다', () => {
    expect(popAllPreviewRows([], TODAY)).toEqual([]);
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd popspot-frontend && npx vitest run src/lib/popAllPreview.test.ts
```

Expected: FAIL — `Failed to resolve import "./popAllPreview"`

- [ ] **Step 3: 구현을 쓴다**

`popspot-frontend/src/lib/popAllPreview.ts`:

```ts
import type { PopupStore } from '@/types/popup';
import { CATEGORIES, classifyCategory, type CategoryCode } from './popupSlices';
import { popupCoverUrl } from './popupCover';

/** 미리보기에 세울 줄 수. 벤토 타일 높이에 네 줄이 들어간다. */
export const PREVIEW_ROW_COUNT = 4;
/** 한 줄에 담을 팝업 수. 가로 스크롤로 훑는다. */
export const PREVIEW_PER_ROW = 10;
/** 이보다 적으면 줄을 만들지 않는다 — 스크롤할 것이 없는 줄은 줄이 아니다. */
const MIN_ROW_SIZE = 3;

export interface PreviewRow {
  code: CategoryCode;
  popups: PopupStore[];
}

/**
 * 하루 단위로 바뀌는 정수 씨앗.
 *
 * <p>난수를 쓰지 않는 이유는 두 가지다. 렌더할 때마다 달라지면 스크롤만 해도 목록이 바뀌어
 * 사람이 방금 본 것을 다시 찾지 못하고, 테스트로 고정할 수도 없다. 날짜를 씨앗으로 삼으면
 * <b>같은 날은 항상 같고 다음 날은 반드시 다르다</b>.
 */
function daySeed(today: Date): number {
  return Math.floor(today.getTime() / 86_400_000);
}

/** 배열을 offset 부터 순환하며 count 개 뽑는다. count 가 길이보다 크면 있는 만큼만. */
function cyclicSlice<T>(items: T[], offset: number, count: number): T[] {
  const n = Math.min(count, items.length);
  const start = ((offset % items.length) + items.length) % items.length;
  return Array.from({ length: n }, (_, i) => items[(start + i) % items.length]);
}

/**
 * POP-ALL 미리보기의 줄들 — <b>카테고리마다 한 줄, 한 줄에 열 곳</b>.
 *
 * <p><b>왜 카테고리별인가.</b> 이 자리가 맡은 일은 "전체가 있다" 는 <i>주장</i>이 아니라
 * 넓이를 <b>보여주는</b> 것이다. 인기순 열 곳을 늘어놓으면 옆 자리(POP-LOOK 랭킹)와 같은
 * 화면이 되고, 최신순으로 늘어놓으면 그 옆(최근 오픈한 팝업 레일)과 같아진다. 축을
 * 카테고리로 잡아야 세 자리가 서로 다른 일을 한다.
 *
 * <p><b>왜 매일 도는가.</b> 고정 목록이면 재방문자에게 두 번째 방문부터 죽은 칸이 된다.
 * 카테고리 조합과 줄 안의 시작점을 모두 날짜로 돌려, 같은 날은 안정적이고 다음 날은 새롭다.
 *
 * <p><b>사진 없는 팝업은 뺀다.</b> 이 자리의 약속이 "사진과 제목" 이라서다. 회색 상자로
 * 채운 줄은 넓이를 보여주기는커녕 데이터가 부실하다는 인상만 남긴다.
 */
export function popAllPreviewRows(
  popups: PopupStore[],
  today: Date,
  options: { rowCount?: number; perRow?: number } = {},
): PreviewRow[] {
  const rowCount = options.rowCount ?? PREVIEW_ROW_COUNT;
  const perRow = options.perRow ?? PREVIEW_PER_ROW;

  const byCategory = new Map<CategoryCode, PopupStore[]>();
  for (const p of popups) {
    if (!popupCoverUrl(p)) continue;
    const code = classifyCategory(p.category);
    // 'other' 는 사람이 읽을 줄 제목이 없다 — 축이 아니라 나머지 통이다.
    if (code === 'other') continue;
    const bucket = byCategory.get(code);
    if (bucket) bucket.push(p);
    else byCategory.set(code, [p]);
  }

  // CATEGORIES 순서를 따라 후보를 세운다 — Map 삽입 순서(=데이터 도착 순서)에 기대면
  // 백엔드 정렬이 바뀔 때 화면이 조용히 따라 바뀐다.
  const candidates = CATEGORIES.map((c) => c.code).filter((code) => {
    const bucket = byCategory.get(code);
    return bucket !== undefined && bucket.length >= MIN_ROW_SIZE;
  });
  if (candidates.length === 0) return [];

  const seed = daySeed(today);
  const rows: PreviewRow[] = [];
  for (let i = 0; i < Math.min(rowCount, candidates.length); i += 1) {
    const code = candidates[(seed + i) % candidates.length];
    // id 내림차순으로 안정화한 뒤 잘라야, 같은 씨앗이 항상 같은 열 곳을 준다.
    const bucket = [...(byCategory.get(code) ?? [])].sort((a, b) => b.id - a.id);
    // 줄마다 시작점을 다르게 흩는다(x7) — 안 그러면 네 줄이 같은 자리에서 함께 움직인다.
    rows.push({ code, popups: cyclicSlice(bucket, seed + i * 7, perRow) });
  }
  return rows;
}
```

- [ ] **Step 4: 통과를 확인한다**

```bash
cd popspot-frontend && npx vitest run src/lib/popAllPreview.test.ts && npx tsc --noEmit -p tsconfig.json
```

Expected: 11 tests PASS, tsc 무출력

**주의:** 「하루가 지나면 카테고리 조합도 달라진다」는 후보가 다섯일 때 `(seed+i)%5` 가 하루 뒤 한 칸 밀리므로 반드시 달라진다. 통과하지 않으면 테스트의 카테고리 수가 `rowCount` 이하로 줄어든 것이다 — 테스트를 고치고 구현은 건드리지 말 것.

- [ ] **Step 5: 커밋**

```bash
npx prettier --write popspot-frontend/src/lib/popAllPreview.ts popspot-frontend/src/lib/popAllPreview.test.ts
git add popspot-frontend/src/lib/popAllPreview.ts popspot-frontend/src/lib/popAllPreview.test.ts
git commit -m "feat(popall): 미리보기를 카테고리별 열 곳으로, 매일 다르게 뽑는다"
```

---

### Task 3: 모달 목록 계산 (검색·필터·정렬·페이지네이션·완화 제안)

**Files:**
- Create: `popspot-frontend/src/lib/popAllQuery.ts`
- Test: `popspot-frontend/src/lib/popAllQuery.test.ts`

**Interfaces:**
- Consumes: `classifyCategory`, `CategoryCode`, `parseDate` (`src/lib/popupSlices.ts`), `classifyRegion`, `RegionCode` (`src/lib/regions.ts`), `popupBadge` (Task 1), `PopupStore`
- Produces: `PopAllSort`, `PopAllBadgeFilter`, `PopAllQuery`, `RelaxSuggestion`, `PopAllResult`, `EMPTY_POP_ALL_QUERY`, `POP_ALL_PAGE_SIZE`, `runPopAllQuery(popups, query, today)`

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`popspot-frontend/src/lib/popAllQuery.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { EMPTY_POP_ALL_QUERY, POP_ALL_PAGE_SIZE, runPopAllQuery } from './popAllQuery';
import type { PopAllQuery } from './popAllQuery';
import type { PopupStore } from '@/types/popup';

const TODAY = new Date(2026, 7, 26);

function popup(id: number, overrides: Partial<PopupStore> = {}): PopupStore {
  return {
    id,
    name: '팝업 ' + id,
    location: '성수동',
    status: '',
    viewCount: 0,
    category: '패션',
    startDate: '2026-08-01',
    endDate: '2026-12-31',
    ...overrides,
  } as PopupStore;
}

function q(overrides: Partial<PopAllQuery> = {}): PopAllQuery {
  return { ...EMPTY_POP_ALL_QUERY, ...overrides };
}

describe('runPopAllQuery — 검색', () => {
  it('검색어가 이름에 들어 있는 팝업만 남긴다', () => {
    const pool = [popup(1, { name: '누데이크 성수' }), popup(2, { name: '무신사 스탠다드' })];
    expect(runPopAllQuery(pool, q({ keyword: '누데이크' }), TODAY).items.map((p) => p.id)).toEqual([
      1,
    ]);
  });

  it('검색어는 대소문자를 가리지 않는다', () => {
    const pool = [popup(1, { name: 'NUDAKE Seongsu' })];
    expect(runPopAllQuery(pool, q({ keyword: 'nudake' }), TODAY).total).toBe(1);
  });

  it('검색어는 영문 이름도 함께 본다', () => {
    // 사이트가 3개 국어를 쓰는데 한국어 이름만 검색하면 영어 화면에서 검색이 죽는다.
    const pool = [popup(1, { name: '누데이크', nameEn: 'Nudake' })];
    expect(runPopAllQuery(pool, q({ keyword: 'Nudake' }), TODAY).total).toBe(1);
  });

  it('검색어는 장소도 함께 본다', () => {
    const pool = [popup(1, { name: '무신사', location: '더현대 서울' })];
    expect(runPopAllQuery(pool, q({ keyword: '더현대' }), TODAY).total).toBe(1);
  });

  it('앞뒤 공백만 있는 검색어는 아무것도 거르지 않는다', () => {
    const pool = [popup(1), popup(2)];
    expect(runPopAllQuery(pool, q({ keyword: '   ' }), TODAY).total).toBe(2);
  });
});

describe('runPopAllQuery — 필터', () => {
  it('지역으로 거른다', () => {
    const pool = [popup(1, { location: '성수동' }), popup(2, { location: '홍대입구' })];
    expect(runPopAllQuery(pool, q({ region: 'seongsu' }), TODAY).items.map((p) => p.id)).toEqual([
      1,
    ]);
  });

  it('카테고리로 거른다', () => {
    const pool = [popup(1, { category: '패션' }), popup(2, { category: '뷰티' })];
    expect(runPopAllQuery(pool, q({ category: 'beauty' }), TODAY).items.map((p) => p.id)).toEqual([
      2,
    ]);
  });

  it('마감 임박만 골라낸다', () => {
    const pool = [popup(1, { endDate: '2026-08-28' }), popup(2, { endDate: '2026-12-31' })];
    expect(
      runPopAllQuery(pool, q({ badge: 'closingSoon' }), TODAY).items.map((p) => p.id),
    ).toEqual([1]);
  });

  it('오늘 오픈만 골라낸다', () => {
    const pool = [popup(1, { startDate: '2026-08-26' }), popup(2, { startDate: '2026-08-01' })];
    expect(
      runPopAllQuery(pool, q({ badge: 'openingToday' }), TODAY).items.map((p) => p.id),
    ).toEqual([1]);
  });

  it('조건 두 개를 걸면 둘 다 만족하는 것만 남는다', () => {
    const pool = [
      popup(1, { location: '성수동', category: '패션' }),
      popup(2, { location: '성수동', category: '뷰티' }),
      popup(3, { location: '홍대입구', category: '패션' }),
    ];
    expect(
      runPopAllQuery(pool, q({ region: 'seongsu', category: 'fashion' }), TODAY).items.map(
        (p) => p.id,
      ),
    ).toEqual([1]);
  });
});

describe('runPopAllQuery — 정렬', () => {
  it('최신순은 시작일이 늦은 것을 앞에 둔다', () => {
    const pool = [popup(1, { startDate: '2026-08-01' }), popup(2, { startDate: '2026-08-20' })];
    expect(runPopAllQuery(pool, q({ sort: 'latest' }), TODAY).items.map((p) => p.id)).toEqual([
      2, 1,
    ]);
  });

  it('마감임박순은 종료일이 빠른 것을 앞에 둔다', () => {
    const pool = [popup(1, { endDate: '2026-12-31' }), popup(2, { endDate: '2026-08-28' })];
    expect(runPopAllQuery(pool, q({ sort: 'deadline' }), TODAY).items.map((p) => p.id)).toEqual([
      2, 1,
    ]);
  });

  it('마감임박순에서 종료일을 모르는 팝업은 맨 뒤로 보낸다', () => {
    // 날짜를 모르는 것을 "가장 급한 것" 으로 올려 보내면 목록의 첫 화면이 통째로 거짓말이 된다.
    const pool = [popup(1, { endDate: undefined }), popup(2, { endDate: '2026-08-28' })];
    expect(runPopAllQuery(pool, q({ sort: 'deadline' }), TODAY).items.map((p) => p.id)).toEqual([
      2, 1,
    ]);
  });

  it('인기순은 조회수가 높은 것을 앞에 둔다', () => {
    const pool = [popup(1, { viewCount: 5 }), popup(2, { viewCount: 50 })];
    expect(runPopAllQuery(pool, q({ sort: 'popular' }), TODAY).items.map((p) => p.id)).toEqual([
      2, 1,
    ]);
  });
});

describe('runPopAllQuery — 페이지네이션', () => {
  const pool = Array.from({ length: POP_ALL_PAGE_SIZE * 2 + 5 }, (_, i) => popup(i + 1));

  it('한 페이지에 정해진 수만큼만 담는다', () => {
    expect(runPopAllQuery(pool, q(), TODAY).items).toHaveLength(POP_ALL_PAGE_SIZE);
  });

  it('전체 개수와 페이지 수를 함께 알려준다', () => {
    const r = runPopAllQuery(pool, q(), TODAY);
    expect(r.total).toBe(pool.length);
    expect(r.totalPages).toBe(3);
  });

  it('마지막 페이지에는 남은 만큼만 담는다', () => {
    expect(runPopAllQuery(pool, q({ page: 3 }), TODAY).items).toHaveLength(5);
  });

  it('페이지 번호가 범위를 넘으면 마지막 페이지로 당긴다', () => {
    // 필터를 걸어 결과가 줄었을 때 빈 화면을 보여주는 대신 볼 것이 있는 곳으로 데려간다.
    const r = runPopAllQuery(pool, q({ page: 99 }), TODAY);
    expect(r.page).toBe(3);
    expect(r.items).toHaveLength(5);
  });

  it('페이지 번호가 1보다 작으면 첫 페이지로 올린다', () => {
    expect(runPopAllQuery(pool, q({ page: 0 }), TODAY).page).toBe(1);
  });

  it('결과가 없어도 페이지 수는 1이다', () => {
    expect(runPopAllQuery([], q(), TODAY).totalPages).toBe(1);
  });
});

describe('runPopAllQuery — 빈 결과 안내', () => {
  it('결과가 없으면 어떤 조건을 풀었을 때 몇 곳이 되는지 알려준다', () => {
    const pool = [popup(1, { location: '성수동', category: '패션' })];
    const r = runPopAllQuery(pool, q({ region: 'seongsu', category: 'beauty' }), TODAY);
    expect(r.total).toBe(0);
    expect(r.relaxSuggestions).toEqual([{ field: 'category', count: 1 }]);
  });

  it('조건을 풀어도 결과가 없으면 그 조건은 제안하지 않는다', () => {
    // "이걸 풀면 나온다" 고 해놓고 눌렀는데 또 0이면, 안내가 아니라 두 번째 막다른 길이다.
    const pool = [popup(1, { location: '성수동', category: '패션' })];
    const r = runPopAllQuery(pool, q({ region: 'hongdae', category: 'beauty' }), TODAY);
    expect(r.relaxSuggestions).toEqual([]);
  });

  it('많이 나오는 조건을 먼저 제안한다', () => {
    const pool = [
      popup(1, { location: '성수동', category: '뷰티' }),
      popup(2, { location: '홍대입구', category: '패션' }),
      popup(3, { location: '홍대입구', category: '패션' }),
    ];
    const r = runPopAllQuery(pool, q({ region: 'seongsu', category: 'fashion' }), TODAY);
    expect(r.relaxSuggestions).toEqual([
      { field: 'region', count: 2 },
      { field: 'category', count: 1 },
    ]);
  });

  it('결과가 있으면 완화 제안을 하지 않는다', () => {
    const pool = [popup(1)];
    expect(runPopAllQuery(pool, q(), TODAY).relaxSuggestions).toEqual([]);
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd popspot-frontend && npx vitest run src/lib/popAllQuery.test.ts
```

Expected: FAIL — `Failed to resolve import "./popAllQuery"`

- [ ] **Step 3: 구현을 쓴다**

`popspot-frontend/src/lib/popAllQuery.ts`:

```ts
import type { PopupStore } from '@/types/popup';
import { classifyCategory, parseDate, type CategoryCode } from './popupSlices';
import { classifyRegion, type RegionCode } from './regions';
import { popupBadge } from './popupBadges';

/** 한 페이지에 담는 칸 수. 4열 x 6줄 — 스크롤 한 번에 끝나는 분량. */
export const POP_ALL_PAGE_SIZE = 24;

export type PopAllSort = 'latest' | 'deadline' | 'popular';
export type PopAllBadgeFilter = 'closingSoon' | 'openingToday' | null;

export interface PopAllQuery {
  keyword: string;
  region: RegionCode | null;
  category: CategoryCode | null;
  badge: PopAllBadgeFilter;
  sort: PopAllSort;
  /** 1부터 센다. */
  page: number;
}

/** 결과가 0일 때 "이 조건 하나만 풀면 N곳" 을 담는다. */
export interface RelaxSuggestion {
  field: 'keyword' | 'region' | 'category' | 'badge';
  count: number;
}

export interface PopAllResult {
  items: PopupStore[];
  total: number;
  totalPages: number;
  /** 범위를 벗어난 요청을 당겨 넣은 뒤의 실제 페이지. */
  page: number;
  relaxSuggestions: RelaxSuggestion[];
}

export const EMPTY_POP_ALL_QUERY: PopAllQuery = {
  keyword: '',
  region: null,
  category: null,
  badge: null,
  sort: 'latest',
  page: 1,
};

/** 검색이 훑는 칸들. 3개 국어 이름과 장소까지 본다. */
function haystack(p: PopupStore): string {
  return [p.name, p.nameEn, p.nameJa, p.location, p.locationEn, p.locationJa, p.category]
    .filter(Boolean)
    .join(' ')
    .toLowerCase();
}

function matches(p: PopupStore, query: PopAllQuery, today: Date): boolean {
  const kw = query.keyword.trim().toLowerCase();
  if (kw && !haystack(p).includes(kw)) return false;
  if (query.region && classifyRegion(p.location) !== query.region) return false;
  if (query.category && classifyCategory(p.category) !== query.category) return false;
  if (query.badge) {
    const badge = popupBadge(p.startDate, p.endDate, today);
    if (badge?.kind !== query.badge) return false;
  }
  return true;
}

const time = (v: string | null | undefined, fallback: number): number => {
  const d = parseDate(v ?? null);
  return d ? d.getTime() : fallback;
};

/**
 * 정렬 세 축 — 레일(HomeClient)이 쓰는 것과 같은 비교자다.
 *
 * <p>동점 처리를 id 로 안정화하는 것이 중요하다. 크롤로 들어온 팝업 다수가 viewCount=0 이라,
 * 안정화하지 않으면 페이지를 넘길 때마다 순서가 흔들려 같은 팝업을 두 번 보거나 아예 못 본다.
 */
function sortItems(list: PopupStore[], sort: PopAllSort): PopupStore[] {
  const out = [...list];
  if (sort === 'deadline') {
    // 종료일 미상은 Infinity 로 맨 뒤 — 모르는 것을 가장 급한 것으로 올리지 않는다.
    out.sort(
      (a, b) =>
        time(a.endDate, Infinity) - time(b.endDate, Infinity) ||
        (b.viewCount || 0) - (a.viewCount || 0) ||
        b.id - a.id,
    );
  } else if (sort === 'latest') {
    out.sort((a, b) => time(b.startDate, -Infinity) - time(a.startDate, -Infinity) || b.id - a.id);
  } else {
    out.sort((a, b) => (b.viewCount || 0) - (a.viewCount || 0) || b.id - a.id);
  }
  return out;
}

/** 조건 하나를 지운 질의. 완화 제안을 세기 위한 것. */
function without(query: PopAllQuery, field: RelaxSuggestion['field']): PopAllQuery {
  if (field === 'keyword') return { ...query, keyword: '' };
  return { ...query, [field]: null };
}

/**
 * 결과가 0일 때 <b>다음에 무엇을 누르면 되는지</b>를 계산한다.
 *
 * <p>빈 화면에 "결과가 없습니다" 만 띄우는 것은 막다른 길이다. 목록이 통째로 메모리에 있으니
 * 조건을 하나씩 풀어 본 결과를 <b>실제로 세어서</b> 알려줄 수 있다 — 추측이 아니라 사실이다.
 * 풀어도 0인 조건은 제안하지 않는다. 눌렀는데 또 0이면 안내가 아니라 두 번째 막다른 길이다.
 */
function relaxSuggestions(
  popups: PopupStore[],
  query: PopAllQuery,
  today: Date,
): RelaxSuggestion[] {
  const active: RelaxSuggestion['field'][] = [];
  if (query.keyword.trim()) active.push('keyword');
  if (query.region) active.push('region');
  if (query.category) active.push('category');
  if (query.badge) active.push('badge');
  // 조건이 하나뿐이면 그걸 푸는 것은 "필터 없음" 이라, 제안이 아니라 초기화 버튼의 일이다.
  if (active.length < 2) return [];

  return active
    .map((field) => ({
      field,
      count: popups.filter((p) => matches(p, without(query, field), today)).length,
    }))
    .filter((s) => s.count > 0)
    .sort((a, b) => b.count - a.count);
}

/**
 * POP-ALL 모달이 한 페이지에 그릴 것을 통째로 계산한다.
 *
 * <p>홈이 이미 전체 목록을 메모리에 들고 있어({@code /api/popups}) 서버를 다시 부르지 않는다.
 * 그래서 타이핑마다 즉시 결과가 나오고, 디바운스도 로딩 상태도 필요 없다.
 */
export function runPopAllQuery(
  popups: PopupStore[],
  query: PopAllQuery,
  today: Date,
): PopAllResult {
  const filtered = popups.filter((p) => matches(p, query, today));
  const total = filtered.length;
  const totalPages = Math.max(1, Math.ceil(total / POP_ALL_PAGE_SIZE));
  const page = Math.min(Math.max(1, Math.floor(query.page) || 1), totalPages);
  const start = (page - 1) * POP_ALL_PAGE_SIZE;

  return {
    items: sortItems(filtered, query.sort).slice(start, start + POP_ALL_PAGE_SIZE),
    total,
    totalPages,
    page,
    relaxSuggestions: total === 0 ? relaxSuggestions(popups, query, today) : [],
  };
}
```

- [ ] **Step 4: 통과를 확인한다**

```bash
cd popspot-frontend && npx vitest run src/lib/popAllQuery.test.ts && npx tsc --noEmit -p tsconfig.json
```

Expected: 24 tests PASS, tsc 무출력

- [ ] **Step 5: 커밋**

```bash
npx prettier --write popspot-frontend/src/lib/popAllQuery.ts popspot-frontend/src/lib/popAllQuery.test.ts
git add popspot-frontend/src/lib/popAllQuery.ts popspot-frontend/src/lib/popAllQuery.test.ts
git commit -m "feat(popall): 검색·필터·정렬·페이지네이션을 네트워크 없이 계산한다"
```

---


### Task 4: 카드에 「이미 본 곳」을 넣고, 이미 있는 D-day 배지가 급한 것을 구분하게 한다

**Files:**
- Modify: `popspot-frontend/src/components/main/PopupCard.tsx` (12행 import, 75행 `dday` 계산, 103행 래퍼 className, 131행 배지)

**먼저 알아야 할 것 — 배지는 이미 있다.**

`PopupCard` 는 **이미** 왼쪽 위에 D-day 배지를 그리고 있다(131행). 그러니 새 `badge` prop 을 더하면 같은 자리에 두 개가 겹친다. 문제는 배지가 없는 것이 아니라 **배지가 급한 것을 구분하지 못하는 것**이다:

- `ddayBadge` (`src/lib/dday.ts:38`)는 종료일이 읽히는 **모든** 팝업에 배지를 준다 — `D-127` 도 배지를 단다
- 색은 끝났으면 회색, 아니면 **전부 라임** — 그래서 화면에서 `D-1` 과 `D-127` 이 똑같이 생겼다
- 「오늘 오픈」 은 아예 없다(종료일만 보므로 시작일을 모른다)

그래서 이 태스크는 prop 을 더하는 대신 **있는 배지를 Task 1 의 판정으로 갈아끼운다.** 카드를 쓰는 네 화면(홈 레일 · 랭킹 모달 · 음악 탭 · 상세)에 함께 적용되는데, 그 넷 모두에서 "D-1 이 D-127 과 같아 보이는" 것은 개선할 문제이지 지킬 동작이 아니다.

**`dday.ts` 를 떼는 김에.** `src/lib/dday.ts` 는 로컬 `setHours` 를 써서 Vercel(UTC)에서 KST 00:00–09:00 사이 하루가 밀린다 — 이 저장소가 다른 곳에서는 이미 쓰지 않기로 한 모듈이다. 여기서도 `kstTodayStart()` 기준인 Task 1 의 `popupBadge` 로 바꾼다.

**Interfaces:**
- Consumes: `popupBadge`, `PopupBadge` (Task 1), `kstTodayStart` (`src/lib/popupSlices.ts`)
- Produces: `PopupCardProps` 에 `seen?: boolean` 하나만 추가. **`badge` prop 은 만들지 않는다.**

- [ ] **Step 1: 「이미 본 곳」 prop 을 더한다**

`PopupCardProps`(51-57행)에 한 항목:

```tsx
  /**
   * 이 사용자가 이미 상세를 열어 본 팝업인가. 몇백 곳을 훑는 화면(POP-ALL)에서만 넘긴다 —
   * 본 것과 안 본 것이 구분되지 않으면 스크롤을 내려도 진도가 나가는 느낌이 없다.
   */
  seen?: boolean;
```

시그니처(59행):

```tsx
export function PopupCard({ popup, onClick, onWish, wished, className, seen }: PopupCardProps) {
```

래퍼 `cn`(103행)의 두 인자 사이에 한 줄을 끼운다. **흐리게만 하고 끝내지 않는다** — 그러면 "고장난 카드" 로 읽힌다. hover 에서 원래대로 돌아오게 해서 "본 것이지 못 쓰는 것이 아니다" 를 알린다:

```tsx
      className={cn(
        'group relative flex w-full min-w-0 cursor-pointer flex-col overflow-hidden rounded-2xl border border-gray-200 bg-white text-left shadow-sm transition hover:-translate-y-1 hover:shadow-lg focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-lime-400 sm:w-[220px] sm:shrink-0 dark:border-white/10 dark:bg-white/[0.04]',
        seen && 'opacity-60 saturate-[0.6] hover:opacity-100 hover:saturate-100',
        className,
      )}
```

- [ ] **Step 2: 배지 계산을 Task 1 것으로 바꾼다**

12행의 `import { ddayBadge } from '@/lib/dday';` 를 지우고:

```tsx
import { popupBadge } from '@/lib/popupBadges';
import { kstTodayStart } from '@/lib/popupSlices';
```

75행 `const dday = ddayBadge(popup.endDate);` 를:

```tsx
  // 시작일도 함께 본다 — 종료일만 보던 예전 배지는 「오늘 오픈」을 알 수 없었고,
  // D-1 과 D-127 을 같은 색으로 칠했다. 기준 시각은 KST 달력 날짜다(dday.ts 의
  // 로컬 setHours 는 Vercel UTC 에서 KST 00:00~09:00 동안 하루를 밀어낸다).
  const badge = popupBadge(popup.startDate, popup.endDate, kstTodayStart());
```

- [ ] **Step 3: 배지를 다시 그린다**

131-141행의 `{dday && (...)}` 블록을 통째로 바꾼다. 주석(54.6% 가 만채도로 칠해지던 문제)은 **살려 둔다** — 새 규칙이 그 문제의 답이므로 왜 이 색 배분인지가 거기 적혀 있다:

```tsx
        {/* 이번 계절에 마감하는 카드를 만채도로 칠했더니 진행 중 588개 중 321개(54.6%)가
            해당했다 — 절반 넘게 칠해지면 그건 신호가 아니라 배경이다. 그래서 지금은
            사흘 이내(popupBadges 의 CLOSING_SOON_DAYS)만 빨강으로 튀고, 나머지는
            눈에 띄지 않는 흑백으로 남긴다. 실제로 급한 것만 색을 쓴다. */}
        {badge && (
          <span
            className={cn(
              'absolute left-2.5 top-2.5 rounded-full px-2.5 py-1 text-[11px] font-bold',
              badge.kind === 'closingSoon' && 'bg-red-500 text-white',
              badge.kind === 'openingToday' && 'bg-lime-300 text-ink-900',
              badge.kind === 'upcoming' && 'bg-black/65 text-white backdrop-blur',
            )}
          >
            {badge.kind === 'closingSoon'
              ? `${t('popall.badgeDday')}${badge.dday}`
              : badge.kind === 'openingToday'
                ? t('popall.badgeToday')
                : t('popall.badgeUpcoming')}
          </span>
        )}
```

**여기서 배지가 사라지는 카드가 생긴다.** 예전에는 `D-127` 도 배지를 달았지만 이제 안 단다. 의도한 것이다 — 넉 달 남은 팝업에 카운트다운을 붙이는 것은 정보가 아니라 잡음이고, 그 잡음이 진짜 `D-1` 을 묻는다.

- [ ] **Step 4: `misc.cardDday*` 키가 아직 쓰이는지 본다**

```bash
cd popspot-frontend && grep -rn "misc.cardDdayPrefix\|misc.cardDdaySuffix\|misc.cardEnded\|card.today" src/ app/ | grep -v i18n.tsx
```

남은 사용처가 없으면 세 로케일에서 지운다. 있으면 그대로 둔다.

- [ ] **Step 5: 전부 통과하는지 확인한다**

```bash
cd popspot-frontend && npx tsc --noEmit -p tsconfig.json && npx vitest run
```

Expected: tsc 무출력, 기존 테스트 전부 PASS. `seen` 은 선택 prop 이라 네 호출부가 그대로 통과해야 한다.

**주의:** Task 5(i18n)를 먼저 해야 `popall.badgeDday` 등이 존재한다. 순서를 지킨다.

- [ ] **Step 6: 커밋**

```bash
npx prettier --write popspot-frontend/src/components/main/PopupCard.tsx
git add popspot-frontend/src/components/main/PopupCard.tsx
git commit -m "feat(popall): 카드에 이미 본 곳 표시를 넣고 배지가 급한 것만 칠하게 한다"
```

---


### Task 5: 사전(i18n) 키를 세 언어에 넣는다

**Files:**
- Modify: `popspot-frontend/src/lib/i18n.tsx` (`ko` 39행~, `en` 822행~, `ja` 1610행~)

**Interfaces:**
- Produces: 아래 28개 `MessageKey`. `_localeParity`(2406행)가 셋 중 하나라도 빠지면 `tsc` 를 깨뜨린다.

- [ ] **Step 1: ko 블록에 넣는다**

```ts
'popall.title': 'POP-ALL',
'popall.tagline': '오늘 서울, 갈 수 있는 곳 전부',
'popall.countSuffix': '곳 전체 보기',
'popall.modalDesc': '검색하고, 걸러 보고, 바로 들어가세요',
'popall.searchPlaceholder': '이름이나 장소로 검색',
'popall.allRegions': '전체 지역',
'popall.allCategories': '전체 카테고리',
'popall.filterClosingSoon': '마감 임박',
'popall.filterOpeningToday': '오늘 오픈',
'popall.sortLatest': '최신순',
'popall.sortDeadline': '마감임박순',
'popall.sortPopular': '인기순',
'popall.resultPrefix': '',
'popall.resultSuffix': '곳',
'popall.badgeDday': 'D-',
'popall.badgeToday': '오늘 오픈',
'popall.badgeUpcoming': '오픈 예정',
'popall.seen': '본 곳',
'popall.recentTitle': '최근 본 팝업',
'popall.recentEmpty': '아직 본 팝업이 없어요',
'popall.recentClear': '기록 지우기',
'popall.emptyTitle': '이 조건에는 없어요',
'popall.relaxKeyword': '검색어 빼고 보기',
'popall.relaxRegion': '지역 빼고 보기',
'popall.relaxCategory': '카테고리 빼고 보기',
'popall.relaxBadge': '기간 조건 빼고 보기',
'popall.resetAll': '조건 전부 지우기',
'popall.kbdHint': '/ 검색 · ←→ 페이지 · Esc 닫기',
```

- [ ] **Step 2: en 블록에 넣는다**

```ts
'popall.title': 'POP-ALL',
'popall.tagline': 'Every pop-up open in Seoul today',
'popall.countSuffix': ' places — browse all',
'popall.modalDesc': 'Search, filter, and jump straight in',
'popall.searchPlaceholder': 'Search by name or place',
'popall.allRegions': 'All areas',
'popall.allCategories': 'All categories',
'popall.filterClosingSoon': 'Closing soon',
'popall.filterOpeningToday': 'Opened today',
'popall.sortLatest': 'Newest',
'popall.sortDeadline': 'Closing first',
'popall.sortPopular': 'Most viewed',
'popall.resultPrefix': '',
'popall.resultSuffix': ' places',
'popall.badgeDday': 'D-',
'popall.badgeToday': 'Opened today',
'popall.badgeUpcoming': 'Opening soon',
'popall.seen': 'Seen',
'popall.recentTitle': 'Recently viewed',
'popall.recentEmpty': 'Nothing viewed yet',
'popall.recentClear': 'Clear history',
'popall.emptyTitle': 'Nothing matches these filters',
'popall.relaxKeyword': 'Drop the search term',
'popall.relaxRegion': 'Drop the area filter',
'popall.relaxCategory': 'Drop the category filter',
'popall.relaxBadge': 'Drop the date filter',
'popall.resetAll': 'Clear all filters',
'popall.kbdHint': '/ search · ←→ pages · Esc close',
```

- [ ] **Step 3: ja 블록에 넣는다**

```ts
'popall.title': 'POP-ALL',
'popall.tagline': '今日ソウルで行けるポップアップ全部',
'popall.countSuffix': '件をすべて見る',
'popall.modalDesc': '検索して、絞って、すぐ入れます',
'popall.searchPlaceholder': '名前や場所で検索',
'popall.allRegions': 'すべてのエリア',
'popall.allCategories': 'すべてのカテゴリー',
'popall.filterClosingSoon': '終了間近',
'popall.filterOpeningToday': '本日オープン',
'popall.sortLatest': '新着順',
'popall.sortDeadline': '終了間近順',
'popall.sortPopular': '人気順',
'popall.resultPrefix': '',
'popall.resultSuffix': '件',
'popall.badgeDday': 'D-',
'popall.badgeToday': '本日オープン',
'popall.badgeUpcoming': 'オープン予定',
'popall.seen': '閲覧済み',
'popall.recentTitle': '最近見たポップアップ',
'popall.recentEmpty': 'まだ見たポップアップがありません',
'popall.recentClear': '履歴を消す',
'popall.emptyTitle': 'この条件では見つかりません',
'popall.relaxKeyword': 'キーワードを外す',
'popall.relaxRegion': 'エリアを外す',
'popall.relaxCategory': 'カテゴリーを外す',
'popall.relaxBadge': '期間条件を外す',
'popall.resetAll': '条件をすべて消す',
'popall.kbdHint': '/ 検索 · ←→ ページ · Esc 閉じる',
```

- [ ] **Step 4: 세 언어가 다 찼는지 컴파일러에게 묻는다**

```bash
cd popspot-frontend && npx tsc --noEmit -p tsconfig.json
```

Expected: 무출력. 하나라도 빠지면 `_localeParity` 가 어느 로케일의 어느 키인지 짚어 준다.

- [ ] **Step 5: 커밋**

```bash
npx prettier --write popspot-frontend/src/lib/i18n.tsx
git add popspot-frontend/src/lib/i18n.tsx
git commit -m "feat(popall): POP-ALL 문구를 한국어·영어·일본어로 넣는다"
```

---

### Task 6: 미리보기 타일 — 카테고리별 가로 스크롤

**Files:**
- Create: `popspot-frontend/src/components/main/PopAllPreview.tsx`
- Modify: `popspot-frontend/src/components/main/HomeBento1a.tsx` (첫 타일 87-142행 교체, 여권·일정 타일은 손대지 않는다)

**Interfaces:**
- Consumes: `popAllPreviewRows`, `PreviewRow` (Task 2), `popupCoverUrl`, `CATEGORIES`/`localizedLabel`/`useLocale`
- Produces: `export default function PopAllPreview({ rows, total, onOpenAll, onOpenPopup }: Props)` — `Props = { rows: PreviewRow[]; total: number; onOpenAll: (category?: CategoryCode) => void; onOpenPopup: (id: number) => void }`

**모양 (이 타일이 옆 두 자리와 다른 이유):**

```
┌──────────────────────────────────────────────┐
│ ◎ POP-ALL              오늘 서울, 갈 수 있는 …│
│                                              │
│ 패션 ›   [▣][▣][▣][▣][▣][▣]…  (가로 스크롤 10) │
│ 푸드 ›   [▣][▣][▣][▣][▣][▣]…                  │
│ 캐릭터 › [▣][▣][▣][▣][▣][▣]…                  │
│ 뷰티 ›   [▣][▣][▣][▣][▣][▣]…                  │
│                                              │
│                       [ 1,002곳 전체 보기 → ] │
└──────────────────────────────────────────────┘
```

- 사진 칸: `w-24` 고정, `aspect-[3/4]`, 아래 제목 2줄(`line-clamp-2`, `text-[11px]`)
- 줄: `flex gap-2 overflow-x-auto snap-x snap-mandatory`, 각 칸 `snap-start`
- 줄 제목: 카테고리 라벨 + `ChevronRight` — **누르면 그 카테고리로 필터가 걸린 채 모달이 열린다**(랜딩으로 보내지 않는다)
- 사진 칸을 누르면 그 팝업 상세로 바로 간다

- [ ] **Step 1: 컴포넌트를 만든다**

`PopAllPreview.tsx` 의 핵심(전체 파일은 이 구조를 따른다):

```tsx
'use client';

import Image from 'next/image';
import { ChevronRight, LayoutGrid } from 'lucide-react';
import { motion, useReducedMotion } from 'framer-motion';
import type { PreviewRow } from '@/lib/popAllPreview';
import { popupCoverUrl } from '@/lib/popupCover';
import { CATEGORIES, type CategoryCode } from '@/lib/popupSlices';
import { localizedLabel, useLocale } from '@/lib/i18n';

interface Props {
  rows: PreviewRow[];
  total: number;
  /** 카테고리를 주면 그 조건이 걸린 채로 모달이 열린다. */
  onOpenAll: (category?: CategoryCode) => void;
  onOpenPopup: (id: number) => void;
}

export default function PopAllPreview({ rows, total, onOpenAll, onOpenPopup }: Props) {
  const { t, locale } = useLocale();
  const reduce = useReducedMotion();

  return (
    <div className="flex flex-col rounded-[2rem] border border-black/[0.06] bg-white p-5 text-ink-900 shadow-pop md:p-6 lg:col-span-2 lg:row-span-2 dark:border-transparent dark:bg-ink-900 dark:text-cream-200">
      <header className="mb-1 flex items-center gap-2">
        <LayoutGrid size={18} className="text-lime-600 dark:text-lime-300" />
        <h3 className="text-lg font-black tracking-tight">{t('popall.title')}</h3>
      </header>
      <p className="mb-4 text-xs text-ink-500 dark:text-cream-200/55">{t('popall.tagline')}</p>

      <div className="flex-1 space-y-3">
        {rows.length === 0
          ? [...Array(4)].map((_, i) => (
              <div key={i} className="flex gap-2">
                {[...Array(6)].map((_, j) => (
                  <div
                    key={j}
                    className="h-28 w-24 shrink-0 animate-pulse rounded-xl bg-black/10 dark:bg-white/10"
                  />
                ))}
              </div>
            ))
          : rows.map((row, rowIdx) => (
              <motion.div
                key={row.code}
                initial={reduce ? false : { opacity: 0, y: 10 }}
                animate={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.35, delay: reduce ? 0 : rowIdx * 0.06, ease: 'easeOut' }}
              >
                <button
                  type="button"
                  onClick={() => onOpenAll(row.code)}
                  className="mb-1.5 flex items-center gap-0.5 text-xs font-bold text-ink-600 transition hover:text-lime-600 dark:text-cream-200/70 dark:hover:text-lime-300"
                >
                  {localizedLabel(CATEGORIES.find((c) => c.code === row.code)!, locale)}
                  <ChevronRight size={13} />
                </button>
                <div className="flex snap-x snap-mandatory gap-2 overflow-x-auto pb-1">
                  {row.popups.map((p) => (
                    <button
                      key={p.id}
                      type="button"
                      onClick={() => onOpenPopup(p.id)}
                      className="w-24 shrink-0 snap-start text-left transition hover:-translate-y-0.5"
                    >
                      <div className="relative aspect-[3/4] overflow-hidden rounded-xl bg-cream-300 dark:bg-ink-800">
                        <Image
                          src={popupCoverUrl(p) ?? ''}
                          alt=""
                          fill
                          sizes="96px"
                          className="object-cover"
                        />
                      </div>
                      <span className="mt-1 line-clamp-2 block text-[11px] font-semibold leading-tight">
                        {p.name}
                      </span>
                    </button>
                  ))}
                </div>
              </motion.div>
            ))}
      </div>

      <button
        type="button"
        onClick={() => onOpenAll()}
        className="mt-3 flex items-center justify-center gap-1 rounded-2xl bg-lime-300 py-2.5 text-sm font-black text-ink-900 transition hover:brightness-105"
      >
        {total.toLocaleString()}
        {t('popall.countSuffix')}
        <ChevronRight size={15} />
      </button>
    </div>
  );
}
```

**주의:** `popupCoverUrl(p)` 는 Task 2 에서 이미 걸렀으므로 여기서 null 이 될 수 없다. 그래도 `?? ''` 를 두는 것은 타입 때문이지 방어가 아니다 — 빈 문자열이 실제로 렌더되면 Task 2 의 필터가 깨진 것이니 그때는 여기가 아니라 Task 2 를 고친다.

- [ ] **Step 2: HomeBento1a 의 첫 타일을 갈아끼운다**

`HomeBento1a.tsx` 에서 87-142행(문 4개 타일)을 통째로 지우고 `<PopAllPreview ... />` 하나로 바꾼다. `Props` 를 바꾼다:

```tsx
interface Props {
  rows: PreviewRow[];
  total: number;
  onOpenAll: (category?: CategoryCode) => void;
  onOpenPopup: (id: number) => void;
  onNavigate: (tab: string) => void;
}
```

`CatalogDoor`, `AXIS_ICON`, `doorLabel`, `regionBySlug`/`categoryBySlug`/`brandBySlug`/`periodBySlug`, `localizedPath`, `Link`, `Compass`, `Store`, `MapPin`, `Tag`, `CalendarRange` import 를 지운다. 파일 상단 JSDoc 의 v2.54 문단을 다시 쓴다 — 문 4개가 랜딩으로 보내던 것을 왜 그만뒀는지(재탕) 남긴다.

- [ ] **Step 3: 컴파일과 기존 테스트**

```bash
cd popspot-frontend && npx tsc --noEmit -p tsconfig.json
```

Expected: `HomeClient.tsx` 가 아직 옛 props 를 넘기고 있어 **에러가 난다**. 이건 Task 8에서 배선하며 없어진다. 이 단계에서는 `HomeBento1a`/`PopAllPreview` 자체에 대한 에러가 없는지만 확인한다.

- [ ] **Step 4: 커밋**

```bash
npx prettier --write popspot-frontend/src/components/main/PopAllPreview.tsx popspot-frontend/src/components/main/HomeBento1a.tsx
git add popspot-frontend/src/components/main/PopAllPreview.tsx popspot-frontend/src/components/main/HomeBento1a.tsx
git commit -m "feat(popall): 벤토 히어로를 카테고리별 가로 스크롤 미리보기로 바꾼다"
```

---

### Task 7: 대형 모달 — 그리드·페이지네이션·필터바·최근 본 팝업·빈 결과

**Files:**
- Create: `popspot-frontend/src/features/popup/PopAllModal.tsx`
- Create: `popspot-frontend/src/features/popup/PopAllFilterBar.tsx`
- Create: `popspot-frontend/src/features/popup/PopAllRecentPanel.tsx`

**Interfaces:**
- Consumes: `runPopAllQuery`/`EMPTY_POP_ALL_QUERY`/`PopAllQuery`/`POP_ALL_PAGE_SIZE` (Task 3), `popupBadge` (Task 1), `useHistoryBackedModal`, `readVisits`/`clearVisits`/`RecentVisit` (`src/lib/recentVisits.ts`), `PopupCard` (Task 4), `saveHomeReturnState`, `REGIONS`/`CATEGORIES`/`localizedLabel`
- Produces: `export function PopAllModal({ open, onOpenChange, popups, initialCategory }: PopAllModalProps)` — `PopAllModalProps = { open: boolean; onOpenChange: (open: boolean) => void; popups: PopupStore[]; initialCategory?: CategoryCode | null }`

**레이아웃:**

```
DialogContent size="full"  (max-w-[95vw] h-[90vh])
├ DialogHeader   POP-ALL / 검색하고, 걸러 보고, 바로 들어가세요
├ PopAllFilterBar  [검색창 /]  [지역▾][카테고리▾][마감임박][오늘오픈]  [최신|마감|인기]   결과 43곳
└ flex-1 min-h-0 flex gap-4
  ├ 본문 flex-1 min-w-0 flex flex-col
  │  ├ 그리드 flex-1 overflow-y-auto  (2 / 3 / 4 / 5열, 24칸)
  │  └ 페이지네이션 shrink-0
  └ 사이드 hidden lg:flex w-56 shrink-0 (최근 본 팝업)
```

- [ ] **Step 1: PopAllRecentPanel 을 만든다**

localStorage 는 SSR 에서 못 읽으므로 **반드시 `useEffect` 안에서** 읽는다. 모달이 열릴 때마다 다시 읽어야 상세를 보고 돌아온 뒤 최신 상태가 된다.

```tsx
'use client';

import Image from 'next/image';
import { History, X } from 'lucide-react';
import { useEffect, useState } from 'react';
import { clearVisits, readVisits, type RecentVisit } from '@/lib/recentVisits';
import { useLocale } from '@/lib/i18n';

/** 사이드 패널이 보여줄 최대 개수 — 기록 자체는 무제한으로 쌓인다. */
const PANEL_LIMIT = 12;

export function PopAllRecentPanel({
  open,
  onOpenPopup,
}: {
  open: boolean;
  onOpenPopup: (id: number) => void;
}) {
  const { t } = useLocale();
  const [visits, setVisits] = useState<RecentVisit[]>([]);

  // 열릴 때마다 다시 읽는다 — 상세를 보고 돌아오면 목록이 달라져 있어야 한다.
  useEffect(() => {
    if (open) setVisits(readVisits().slice(0, PANEL_LIMIT));
  }, [open]);

  return (
    <aside className="hidden w-56 shrink-0 flex-col lg:flex">
      <header className="mb-2 flex items-center justify-between">
        <h4 className="flex items-center gap-1.5 text-sm font-black">
          <History size={15} className="text-lime-600 dark:text-lime-300" />
          {t('popall.recentTitle')}
        </h4>
        {visits.length > 0 ? (
          <button
            type="button"
            onClick={() => {
              clearVisits();
              setVisits([]);
            }}
            className="text-[11px] text-ink-400 underline-offset-2 hover:underline dark:text-cream-200/40"
          >
            {t('popall.recentClear')}
          </button>
        ) : null}
      </header>

      {visits.length === 0 ? (
        <p className="rounded-2xl border border-dashed border-black/10 p-4 text-center text-[11px] text-ink-400 dark:border-white/10 dark:text-cream-200/40">
          {t('popall.recentEmpty')}
        </p>
      ) : (
        <ul className="flex-1 space-y-1.5 overflow-y-auto pr-1">
          {visits.map((v) => (
            <li key={v.popupId}>
              <button
                type="button"
                onClick={() => onOpenPopup(v.popupId)}
                className="flex w-full items-center gap-2 rounded-xl p-1.5 text-left transition hover:bg-black/5 dark:hover:bg-white/5"
              >
                <span className="relative h-10 w-10 shrink-0 overflow-hidden rounded-lg bg-cream-300 dark:bg-ink-800">
                  {v.popupImage ? (
                    <Image src={v.popupImage} alt="" fill sizes="40px" className="object-cover" />
                  ) : null}
                </span>
                <span className="line-clamp-2 text-[11px] font-semibold leading-tight">
                  {v.popupName}
                </span>
              </button>
            </li>
          ))}
        </ul>
      )}
    </aside>
  );
}
```

- [ ] **Step 2: PopAllFilterBar 를 만든다**

네이티브 `<select>` 를 쓴다 — 지역이 수십 개라 칩으로 늘어놓으면 필터바가 화면 절반을 먹고, 모바일에서는 OS 가 제 몫의 UI 를 준다. 배지 필터 둘은 토글 버튼.

```tsx
interface FilterBarProps {
  query: PopAllQuery;
  total: number;
  searchRef: React.RefObject<HTMLInputElement | null>;
  onChange: (patch: Partial<PopAllQuery>) => void;
}
```

- 검색 input: `value={query.keyword}`, `onChange={(e) => onChange({ keyword: e.target.value, page: 1 })}`, `ref={searchRef}`
- **필터가 바뀌면 항상 `page: 1` 로 되돌린다.** 3페이지를 보다가 필터를 걸면 결과가 1페이지뿐인데 3페이지를 요청하게 되고, Task 3 이 마지막 페이지로 당겨 주긴 하지만 사용자가 보기엔 페이지가 제멋대로 튄 것이다.
- 지역 `<select>`: `<option value="">{t('popall.allRegions')}</option>` + `REGIONS.map(r => <option key={r.code} value={r.code}>{localizedLabel(r, locale)}</option>)`
- 카테고리 `<select>`: 같은 방식으로 `CATEGORIES`
- 배지 토글 둘: 누르면 `badge: query.badge === 'closingSoon' ? null : 'closingSoon'`
- 정렬: 세 개짜리 세그먼트 버튼
- 오른쪽 끝에 결과 수: `{t('popall.resultPrefix')}{total.toLocaleString()}{t('popall.resultSuffix')}` — `aria-live="polite"` 를 달아 화면 낭독기에도 결과 변화가 전달되게 한다

- [ ] **Step 3: PopAllModal 을 만든다**

```tsx
export function PopAllModal({ open, onOpenChange, popups, initialCategory }: PopAllModalProps) {
  const { t, locale } = useLocale();
  const router = useRouter();
  const reduce = useReducedMotion();
  const searchRef = useRef<HTMLInputElement | null>(null);
  const [query, setQuery] = useState<PopAllQuery>(EMPTY_POP_ALL_QUERY);
  const [seenIds, setSeenIds] = useState<Set<number>>(() => new Set());
  const { onOpenChange: handleOpenChange, notifyNavigatingAway } = useHistoryBackedModal(
    open,
    onOpenChange,
  );

  // 열 때마다 조건을 초기화한다. 지난번 필터가 남아 있으면 "전체 보기" 를 눌렀는데
  // 43곳만 나오는 일이 생긴다 — 이 화면의 이름이 약속하는 것과 정반대다.
  useEffect(() => {
    if (open) {
      setQuery({ ...EMPTY_POP_ALL_QUERY, category: initialCategory ?? null });
      setSeenIds(new Set(readVisits().map((v) => v.popupId)));
    }
  }, [open, initialCategory]);

  const today = useMemo(() => kstTodayStart(), []);
  const result = useMemo(() => runPopAllQuery(popups, query, today), [popups, query, today]);
  const patch = useCallback((p: Partial<PopAllQuery>) => setQuery((q) => ({ ...q, ...p })), []);

  const goDetail = (id: number) => {
    notifyNavigatingAway();
    saveHomeReturnState();
    onOpenChange(false);
    router.replace(localizedPath(`/popup/${id}`, locale));
  };
  ...
}
```

**그리드 + 애니메이션.** 24칸이 한꺼번에 튀어나오면 산만하므로 stagger 를 아주 짧게 준다. 페이지가 바뀌면 `key={result.page}` 로 새로 마운트되어 다시 흐른다:

```tsx
<AnimatePresence mode="wait">
  <motion.div
    key={`${result.page}-${query.sort}-${query.keyword}-${query.region}-${query.category}-${query.badge}`}
    initial={reduce ? false : { opacity: 0 }}
    animate={{ opacity: 1 }}
    exit={reduce ? undefined : { opacity: 0 }}
    transition={{ duration: 0.18 }}
    className="grid grid-cols-2 gap-3 pb-2 sm:grid-cols-3 lg:grid-cols-4 xl:grid-cols-5"
  >
    {result.items.map((p, i) => (
      <motion.div
        key={p.id}
        initial={reduce ? false : { opacity: 0, y: 8 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.28, delay: reduce ? 0 : Math.min(i, 12) * 0.025, ease: 'easeOut' }}
      >
        <PopupCard popup={p} seen={seenIds.has(p.id)} onClick={() => goDetail(p.id)} />
      </motion.div>
    ))}
  </motion.div>
</AnimatePresence>
```

`delay` 를 `Math.min(i, 12)` 로 자르는 이유: 24번째 칸까지 순서대로 밀면 마지막 칸이 0.6초 뒤에 나타난다. 12칸(0.3초)에서 끊으면 흐르는 느낌은 남고 기다림은 사라진다.

**배지는 여기서 그리지 않는다.** Task 4 가 `PopupCard` 안의 기존 D-day 배지를 `popupBadge` 로
갈아끼웠으므로 카드가 스스로 「D-3」·「오늘 오픈」을 그린다. 여기서 또 넘기면 같은 자리에
두 개가 겹친다.

**빈 결과.** 「결과 없음」만 띄우지 않는다 — 눌러서 빠져나갈 문을 함께 준다:

```tsx
{result.total === 0 ? (
  <div className="flex flex-col items-center gap-3 py-16 text-center">
    <SearchX size={32} className="text-ink-300 dark:text-cream-200/30" />
    <p className="text-sm font-bold">{t('popall.emptyTitle')}</p>
    <div className="flex flex-wrap justify-center gap-2">
      {result.relaxSuggestions.map((s) => (
        <button
          key={s.field}
          type="button"
          onClick={() => patch(relaxPatch(s.field))}
          className="rounded-full border border-black/10 px-3 py-1.5 text-xs font-bold transition hover:border-lime-400 hover:bg-lime-300/10 dark:border-white/15"
        >
          {t(RELAX_LABEL[s.field])} ({s.count.toLocaleString()}
          {t('popall.resultSuffix')})
        </button>
      ))}
      <button
        type="button"
        onClick={() => patch({ ...EMPTY_POP_ALL_QUERY })}
        className="rounded-full bg-lime-300 px-3 py-1.5 text-xs font-black text-ink-900"
      >
        {t('popall.resetAll')}
      </button>
    </div>
  </div>
) : ( ...그리드... )}
```

`RELAX_LABEL` 은 모듈 상수:

```tsx
const RELAX_LABEL: Record<RelaxSuggestion['field'], MessageKey> = {
  keyword: 'popall.relaxKeyword',
  region: 'popall.relaxRegion',
  category: 'popall.relaxCategory',
  badge: 'popall.relaxBadge',
};
```

완화는 `keyword` 만 `''`, 나머지는 `null` 이므로:

```tsx
const relaxPatch = (field: RelaxSuggestion['field']): Partial<PopAllQuery> =>
  field === 'keyword' ? { keyword: '', page: 1 } : { [field]: null, page: 1 };
```

`relaxPatch` 는 모듈 함수로 둔다 — 완화는 `keyword` 만 빈 문자열이고 나머지 셋은 null 이라, 이 갈래를 한 곳에 모아 두지 않으면 세 군데에서 각자 틀린다.

**페이지네이션.** 38페이지를 다 그리면 그 줄이 화면을 먹는다. 현재 페이지 주변 ±2 와 처음·끝만 그리고 사이는 `…`:

```tsx
function pageWindow(page: number, totalPages: number): (number | 'gap')[] {
  const pages = new Set<number>([1, totalPages, page - 2, page - 1, page, page + 1, page + 2]);
  const shown = [...pages].filter((p) => p >= 1 && p <= totalPages).sort((a, b) => a - b);
  const out: (number | 'gap')[] = [];
  shown.forEach((p, i) => {
    if (i > 0 && p - shown[i - 1] > 1) out.push('gap');
    out.push(p);
  });
  return out;
}
```

페이지를 바꿀 때 **그리드 스크롤을 맨 위로 올린다** — 안 그러면 2페이지가 중간부터 보인다:

```tsx
const gridRef = useRef<HTMLDivElement | null>(null);
const goPage = (p: number) => {
  patch({ page: p });
  gridRef.current?.scrollTo({ top: 0, behavior: reduce ? 'auto' : 'smooth' });
};
```

- [ ] **Step 4: 컴파일**

```bash
cd popspot-frontend && npx tsc --noEmit -p tsconfig.json
```

Expected: `HomeClient.tsx` 배선 전이라 그쪽 에러만 남고, 새 파일 셋에는 에러가 없어야 한다.

- [ ] **Step 5: 커밋**

```bash
npx prettier --write popspot-frontend/src/features/popup/PopAllModal.tsx popspot-frontend/src/features/popup/PopAllFilterBar.tsx popspot-frontend/src/features/popup/PopAllRecentPanel.tsx
git add popspot-frontend/src/features/popup/PopAll*.tsx
git commit -m "feat(popall): 검색·필터·최근 본 팝업이 있는 전체보기 모달을 만든다"
```

---

### Task 8: 키보드 단축키

**Files:**
- Create: `popspot-frontend/src/features/popup/usePopAllKeyboard.ts`
- Test: `popspot-frontend/src/features/popup/usePopAllKeyboard.test.ts`
- Modify: `popspot-frontend/src/features/popup/PopAllModal.tsx` (훅 연결)

**Interfaces:**
- Produces: `export type PopAllKeyAction = 'focusSearch' | 'prevPage' | 'nextPage' | null`, `export function popAllKeyAction(key: string, target: EventTarget | null): PopAllKeyAction`, `export function usePopAllKeyboard(opts): void`

**결정 하나:** 이 저장소에는 `@testing-library/react` 가 **없다**(실측 확인). 훅을 직접 렌더해 테스트하려고 의존성을 새로 깔지 않는다 — 대신 **판단을 순수 함수로 떼어내** 그것만 테스트하고, 훅은 그 함수를 부르는 다섯 줄짜리 배선으로 남긴다. 이 편이 테스트도 빠르고, 정작 검증하고 싶은 것(어떤 키가 무엇을 하고, 언제 양보하는가)이 정확히 그 함수 안에 있다. `jsdom` 은 있으므로(^29.1.1) 실제 DOM 요소로 검증할 수 있다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

`popspot-frontend/src/features/popup/usePopAllKeyboard.test.ts`:

```ts
// @vitest-environment jsdom
import { describe, expect, it } from 'vitest';
import { popAllKeyAction } from './usePopAllKeyboard';

/** 지정한 태그의 요소를 만들어 문서에 붙인다 — 이벤트 target 자리에 넣기 위한 것. */
function el(tag: string): HTMLElement {
  const node = document.createElement(tag);
  document.body.appendChild(node);
  return node;
}

describe('popAllKeyAction', () => {
  it('슬래시는 검색창으로 포커스를 옮기라는 뜻이다', () => {
    expect(popAllKeyAction('/', el('div'))).toBe('focusSearch');
  });

  it('오른쪽 화살표는 다음 페이지다', () => {
    expect(popAllKeyAction('ArrowRight', el('div'))).toBe('nextPage');
  });

  it('왼쪽 화살표는 이전 페이지다', () => {
    expect(popAllKeyAction('ArrowLeft', el('div'))).toBe('prevPage');
  });

  it('다루지 않는 키에는 아무 일도 시키지 않는다', () => {
    expect(popAllKeyAction('a', el('div'))).toBeNull();
  });

  it('입력칸 안에서는 슬래시를 가로채지 않는다', () => {
    // 이름에 슬래시가 든 팝업을 영영 검색할 수 없게 되는 것이 이 실수의 대가다.
    expect(popAllKeyAction('/', el('input'))).toBeNull();
  });

  it('입력칸 안에서 화살표는 커서 이동이지 페이지 이동이 아니다', () => {
    expect(popAllKeyAction('ArrowRight', el('input'))).toBeNull();
  });

  it('여러 줄 입력칸에서도 양보한다', () => {
    expect(popAllKeyAction('/', el('textarea'))).toBeNull();
  });

  it('선택 상자에서도 양보한다', () => {
    // 화살표로 항목을 고르는 중에 페이지가 넘어가면 고르던 것을 잃는다.
    expect(popAllKeyAction('ArrowRight', el('select'))).toBeNull();
  });

  it('편집 가능한 영역에서도 양보한다', () => {
    const node = el('div');
    node.contentEditable = 'true';
    expect(popAllKeyAction('/', node)).toBeNull();
  });

  it('target 이 없으면 단축키로 본다', () => {
    expect(popAllKeyAction('ArrowRight', null)).toBe('nextPage');
  });
});
```

- [ ] **Step 2: 실패를 확인한다**

```bash
cd popspot-frontend && npx vitest run src/features/popup/usePopAllKeyboard.test.ts
```

Expected: FAIL — `Failed to resolve import "./usePopAllKeyboard"`

- [ ] **Step 3: 구현을 쓴다**

`popspot-frontend/src/features/popup/usePopAllKeyboard.ts`:

```ts
'use client';

import { useEffect, type RefObject } from 'react';

export type PopAllKeyAction = 'focusSearch' | 'prevPage' | 'nextPage' | null;

/** 지금 글자를 받고 있는 칸인가 — 그렇다면 단축키가 끼어들면 안 된다. */
function isTyping(target: EventTarget | null): boolean {
  if (!(target instanceof HTMLElement)) return false;
  const tag = target.tagName;
  return tag === 'INPUT' || tag === 'TEXTAREA' || tag === 'SELECT' || target.isContentEditable;
}

/**
 * 키 하나가 POP-ALL 에서 무엇을 뜻하는가. <b>판단만 하고 아무것도 하지 않는다</b>.
 *
 * <p>훅에서 떼어낸 이유는 이 판단이 전부이기 때문이다 — 어떤 키가 무엇을 하고, <b>언제
 * 양보하는가</b>. 양보 규칙이 특히 중요하다. 글자를 받는 칸 안에서 단축키가 이기면, 이름에
 * 슬래시가 든 팝업을 검색할 수 없고 검색어 안에서 커서를 옮길 수도 없다 — 단축키가 아예
 * 없는 것보다 나쁘다.
 *
 * <p>Esc 는 다루지 않는다. Radix Dialog 가 이미 처리하고 있고, 여기서 또 잡으면 두 번
 * 닫히거나(history 항목이 두 개 소비된다) 서로 막는다.
 */
export function popAllKeyAction(key: string, target: EventTarget | null): PopAllKeyAction {
  if (isTyping(target)) return null;
  if (key === '/') return 'focusSearch';
  if (key === 'ArrowRight') return 'nextPage';
  if (key === 'ArrowLeft') return 'prevPage';
  return null;
}

/** {@link popAllKeyAction} 의 판단을 실제 동작에 잇는 배선. */
export function usePopAllKeyboard({
  open,
  searchRef,
  onPrevPage,
  onNextPage,
}: {
  open: boolean;
  searchRef: RefObject<HTMLInputElement | null>;
  onPrevPage: () => void;
  onNextPage: () => void;
}): void {
  useEffect(() => {
    if (!open) return;
    const onKeyDown = (e: KeyboardEvent) => {
      const action = popAllKeyAction(e.key, e.target);
      if (!action) return;
      // 브라우저 기본 동작(슬래시 빠른검색, 화살표 스크롤)을 막는다.
      e.preventDefault();
      if (action === 'focusSearch') searchRef.current?.focus();
      else if (action === 'nextPage') onNextPage();
      else onPrevPage();
    };
    document.addEventListener('keydown', onKeyDown);
    return () => document.removeEventListener('keydown', onKeyDown);
  }, [open, searchRef, onPrevPage, onNextPage]);
}
```

- [ ] **Step 4: 통과를 확인한다**

```bash
cd popspot-frontend && npx vitest run src/features/popup/usePopAllKeyboard.test.ts && npx tsc --noEmit -p tsconfig.json
```

Expected: 10 tests PASS, tsc 무출력

- [ ] **Step 4b: 테스트가 진짜 지키는지 확인한다(사보타주)**

`popAllKeyAction` 의 `if (isTyping(target)) return null;` 한 줄을 잠시 지우고 다시 돌린다. 「입력칸 안에서는 …」 네 개가 **반드시 실패해야** 한다. 확인했으면 되돌린다. 실패하지 않으면 그 테스트들은 아무것도 지키고 있지 않은 것이다.

- [ ] **Step 5: 모달에 연결하고 힌트를 보여준다**

`PopAllModal` 안에서:

```tsx
usePopAllKeyboard({
  open,
  searchRef,
  onPrevPage: () => result.page > 1 && goPage(result.page - 1),
  onNextPage: () => result.page < result.totalPages && goPage(result.page + 1),
});
```

페이지네이션 줄 오른쪽에 `<span className="hidden text-[11px] text-ink-400 lg:inline dark:text-cream-200/35">{t('popall.kbdHint')}</span>` 을 둔다 — 있는 줄 모르는 단축키는 없는 것과 같다.

- [ ] **Step 6: 커밋**

```bash
npx prettier --write popspot-frontend/src/features/popup/usePopAllKeyboard.ts popspot-frontend/src/features/popup/usePopAllKeyboard.test.ts popspot-frontend/src/features/popup/PopAllModal.tsx
git add popspot-frontend/src/features/popup/usePopAllKeyboard.ts popspot-frontend/src/features/popup/usePopAllKeyboard.test.ts popspot-frontend/src/features/popup/PopAllModal.tsx
git commit -m "feat(popall): 검색 포커스와 페이지 이동 단축키를 붙인다"
```

---

### Task 9: 홈에 배선하고, 숫자를 하나로 통일하고, 죽은 코드를 지운다

**Files:**
- Modify: `popspot-frontend/app/HomeClient.tsx`
- Delete: `popspot-frontend/src/lib/catalogDoors.ts`, `popspot-frontend/src/lib/catalogDoors.test.ts`

**Interfaces:**
- Consumes: 앞의 모든 것

- [ ] **Step 1: POP-ALL 이 쓸 목록을 만든다**

`mappablePopups` 정의 바로 아래에 넣는다:

```tsx
/**
 * POP-ALL 이 보여주는 목록 — <b>갈 수 있고, 같은 행사를 한 번만 세는</b> 것.
 *
 * <p>{@code mappablePopups} 를 {@link groupSameEvent} 로 한 번 더 묶는다. 안 묶으면
 * "스트릿 레스토랑 파이터" 가 이름만 다른 네 줄로 들어와 24칸짜리 한 페이지에서 네 칸을
 * 한 행사가 먹는다. 몇백 곳을 다양하게 보여주는 것이 이 화면의 목적인데 정반대가 된다.
 *
 * <p>조회수는 합산한다 — 네 줄로 쪼개졌던 행사를 대표 한 줄의 조회수로만 평가하면
 * 인기순에서 부당하게 밀린다(레일이 쓰는 {@code dedupedPopups} 와 같은 이유).
 */
const popAllPopups = useMemo(
  () =>
    groupSameEvent(mappablePopups).map((g) => {
      if (g.duplicates.length === 0) return g.lead;
      const merged = g.duplicates.reduce((s, d) => s + (d.viewCount || 0), g.lead.viewCount || 0);
      return { ...g.lead, viewCount: merged };
    }),
  [mappablePopups],
);
```

- [ ] **Step 2: 화면이 말하는 숫자를 이 목록으로 바꾼다**

`mappablePopupCount` 를 `popAllPopups.length` 로 바꾸고 JSDoc 을 갱신한다:

```tsx
/**
 * 화면이 말하는 팝업 수는 <b>이 하나</b>다.
 *
 * <p>세 번 걸러낸 값이다 — 오늘 열려 있고(1,002), 지도에서 찾을 수 있고(850), <b>같은 행사를
 * 한 번만 센</b> 것. 마지막 조건을 v2.55 에 더했다. 같은 행사가 이름만 다른 네 줄로 들어와
 * 있으면 "갈 수 있는 곳" 을 네 곳으로 세는 셈인데, 실제로 갈 수 있는 곳은 한 곳이다.
 *
 * <p>그래서 이 숫자는 850보다 조금 작다. 셀 수 있는 것이 아니라 <b>갈 수 있는 것</b>을
 * 센다는 기존 원칙의 연장이다.
 */
const mappablePopupCount = popAllPopups.length;
```

- [ ] **Step 3: 미리보기 줄과 모달 상태를 만든다**

```tsx
const previewRows = useMemo(() => popAllPreviewRows(popAllPopups, kstTodayStart()), [popAllPopups]);
const [popAllOpen, setPopAllOpen] = useState(false);
const [popAllCategory, setPopAllCategory] = useState<CategoryCode | null>(null);

const openPopAll = useCallback((category?: CategoryCode) => {
  setPopAllCategory(category ?? null);
  setPopAllOpen(true);
}, []);
```

`doors` useMemo 와 `catalogDoors`/`CATALOG_DOOR_COUNT` import 를 지운다.

- [ ] **Step 4: 벤토와 모달을 꽂는다**

`HomeBento1a` 호출부(1699행 근처)를 바꾼다:

```tsx
<HomeBento1a
  rows={previewRows}
  total={mappablePopupCount}
  onOpenAll={openPopAll}
  onOpenPopup={(id) => {
    saveHomeReturnState();
    router.push(localizedPath(`/popup/${id}`, locale));
  }}
  onNavigate={handleTabChange}
/>
```

그리고 `AllTrendingModal` 옆에 모달을 둔다:

```tsx
<PopAllModal
  open={popAllOpen}
  onOpenChange={setPopAllOpen}
  popups={popAllPopups}
  initialCategory={popAllCategory}
/>
```

- [ ] **Step 5: 레일의 「전체보기」가 엉뚱한 모달을 여는 것을 고친다**

1725행 근처, 「최근 오픈한 팝업」 레일의 전체보기가 **랭킹 모달**(`AllTrendingModal`)을 열고 있다. 레일 이름과 여는 화면이 다르다. `openPopAll()` 로 바꿔 POP-ALL 을 열게 한다 — 레일이 훑던 것이 바로 전체이므로 이쪽이 이름과 맞는다.

- [ ] **Step 6: 죽은 코드를 지운다**

```bash
git rm popspot-frontend/src/lib/catalogDoors.ts popspot-frontend/src/lib/catalogDoors.test.ts
```

`bento.catalogTitleSuffix`, `bento.catalogDesc` 를 쓰는 곳이 남았는지 확인하고, 없으면 세 로케일에서 지운다:

```bash
cd popspot-frontend && grep -rn "bento.catalogTitleSuffix\|bento.catalogDesc" src/ app/ | grep -v i18n.tsx
```

- [ ] **Step 7: 전부 통과하는지 본다**

```bash
cd popspot-frontend && npx tsc --noEmit -p tsconfig.json && npx vitest run && npm run lint
```

Expected: tsc 무출력, 모든 테스트 PASS, lint 통과

- [ ] **Step 8: 실제로 눈으로 확인한다**

`.claude/launch.json` 의 dev 서버를 preview_start 로 띄우고 홈을 연다. 확인할 것:

1. 벤토 히어로에 카테고리 줄 넷이 뜨고, 각 줄이 가로로 스크롤되며, 칸마다 **사진과 제목**이 있다
2. 「N곳 전체 보기」를 누르면 모달이 **화면의 95%** 를 채운다
3. 검색창에 글자를 넣으면 **즉시** 결과 수가 바뀐다(네트워크 요청이 안 생기는지 read_network_requests 로 확인)
4. 지역·카테고리를 걸면 결과 수가 따라 바뀌고, 페이지가 1로 돌아간다
5. 조건을 겹쳐 0건을 만들면 「~빼고 보기 (N곳)」 버튼이 뜨고, 눌렀을 때 실제로 N곳이 나온다
6. 상세를 하나 열고 돌아오면 그 카드가 흐려지고 오른쪽 「최근 본 팝업」에 뜬다
7. `/` 로 검색창에 포커스가 가고, `←→` 로 페이지가 넘어간다
8. 모바일 폭(375)에서 사이드 패널이 사라지고 그리드가 2열이 된다
9. 브라우저 뒤로가기가 사이트를 떠나지 않고 모달만 닫는다

- [ ] **Step 9: 커밋**

```bash
npx prettier --write popspot-frontend/app/HomeClient.tsx
git add -A popspot-frontend/
git commit -m "feat(popall): 홈에 POP-ALL 을 붙이고 전체 개수를 행사 단위로 통일한다"
```

---

## Self-Review 메모

- **`PopupCard` 에는 이미 D-day 배지가 있다**(실측, 131행). 그래서 계획 초안의 `badge` prop 은
  같은 자리에 배지 두 개를 겹치게 만들 뻔했다. Task 4 를 「배지를 더한다」에서 「있는 배지를
  Task 1 의 판정으로 갈아끼운다」로 다시 썼다. 진짜 문제는 배지가 없는 것이 아니라 `D-1` 과
  `D-127` 이 같은 색인 것이었다.
- **`PopupCard` 가 금지된 `src/lib/dday.ts` 를 쓰고 있었다**(12행). Task 4 에서 함께 떼어낸다.
- **문서 조립 중 두 번 사고가 났다.** `popupBadge(p.startDate, p.endDate, today)` 라는 문자열이
  Task 3 의 `matches()` 안에도 있어서, 그것을 앵커로 삼은 스크립트가 Task 4~6 을 통째로 지웠다.
  단계마다 「태스크가 아직 9개인가」를 확인하는 가드를 넣고서야 잡혔다. 이 계획을 실행할 때도
  같은 교훈이 적용된다 — <b>고유하지 않은 문자열을 앵커로 쓰지 않는다.</b>
- **Task 1 의 날짜 산술을 실측했다.** KST(UTC+9)에서 `new Date(2026,7,26)` 기준으로 `2026-08-26/27/29/25/30` 이 각각 `0/1/3/-1/4` 가 나오는 것을 node 로 확인했다. `Math.round` 의 허용 오차가 ±12시간이라 UTC CI 에서도 같다.
- **Task 3 의 완화 제안 세 케이스를 손으로 검산했다** — `{region:seongsu, category:beauty}` → `[{category,1}]`, `{region:hongdae, category:beauty}` → `[]`, `{region:seongsu, category:fashion}` → `[{region,2},{category,1}]`.
- **쓰는 코드·지역·카테고리 이름을 전부 확인했다** — `hongdae`, `seongsu`, `fashion`, `beauty`, `character`, `dessert`(키워드 '디저트'), `art`(키워드 '아트').
- **`t()` 는 보간을 지원하지 않는다**(`(key: MessageKey) => string`). 그래서 숫자가 들어가는 자리는 전부 Prefix/Suffix 키 + 문자열 이어붙이기로 썼다 — 기존 `music.moodPopupsSuffix` 등과 같은 방식.
- **`@testing-library/react` 는 없다**(실측). 그래서 Task 8 은 처음부터 순수 함수 `popAllKeyAction` 을 테스트하고, 훅은 배선만 남긴다 — 새 의존성을 깔지 않는다.
- **`custom-scrollbar` 는 어느 CSS 에도 정의돼 있지 않다**(실측). 기존 `AllTrendingModal` 이 쓰고 있지만 아무 일도 하지 않는 죽은 클래스라, 새 코드에는 넣지 않았다. 고치는 것은 이 계획의 일이 아니다.
- **숫자가 바뀐다.** `mappablePopupCount` 가 850에서 행사 단위로 내려간다. 의도된 것이고 Task 9 Step 2 에 이유를 남겼다.
