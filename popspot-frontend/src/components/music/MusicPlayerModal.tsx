"use client";

/**
 * @deprecated 글로벌 플레이어(MusicPlayerProvider + GlobalMusicPlayer)로 대체되었다.
 *             남아있는 import 호환을 위한 빈 컴포넌트만 남겨둔다.
 *             다음 정리에서 안전하게 파일째 삭제 가능.
 */
export default function MusicPlayerModal() {
  return null;
}
