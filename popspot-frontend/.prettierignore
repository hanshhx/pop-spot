node_modules
.next
out
build
dist
coverage
.git
public
*.lock
package-lock.json
yarn.lock
pnpm-lock.yaml
tsconfig.tsbuildinfo
next-env.d.ts
*.svg
*.mp4
*.png
*.jpg
.env*

# 비상 스냅샷은 바이트 단위로 고정된 자료다.
#
# 이 파일의 존재 이유는 "그때 그대로임" 이고, 그것을 sha256 으로 증명한다
# (emergencyPopupData.test.ts). prettier 가 다시 포맷하면 내용은 그대로여도 바이트가
# 바뀌어 그 증명이 깨진다 — 실제로 1d2318e 가 format:check 를 통과시키려다 355,685 →
# 435,403 바이트로 바꿔 놓아 CI 의 테스트가 대신 빨간불이 됐다.
#
# 자료 파일은 사람이 읽을 코드가 아니므로 포맷 대상에서 뺀다.
src/data/emergency/*.json
