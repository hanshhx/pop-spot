# ===== Secrets =====
.env
.env.*
!.env.example
*-secret.properties
*-secret.yml
*.pem
*.key
*.p12
*.jks
popspot.env

# ===== Build / Cache =====
**/build/
**/out/
**/bin/
**/.gradle/
**/.next/
**/node_modules/
**/dist/

# 문서·이미지 생성 도구의 결과물. 원본 자료가 섞일 수 있어 저장소에 올리지 않음.
/output/pdf/

# ===== IDE =====
**/.idea/
**/.vscode/
**/*.iml
**/.classpath
**/.project
**/.settings/

# ===== Logs / Temp =====
**/*.log
**/logs/
**/uploads/
**/.DS_Store
**/Thumbs.db

# ===== OS / Misc =====
**/tsconfig.tsbuildinfo
**/next-env.d.ts.bak

# ===== 임시 빌드/도구 산출물 (커밋 금지) =====
# Codex/수동 검증용 임시 Gradle 빌드 디렉터리 — 정식 build/ 와 구분됨
**/build-codex*/
**/build-final/
**/build-verify/
**/codex-build.init.gradle
# ESLint 임시 리포트
eslint-report*.tmp.json
# 루트에 흩어진 캡처/이미지/압축/작업 폴더 (레포 산출물 아님)
/*.jpg
/screencapture-*.png
/*.zip
/learn/
/팝스팟 앱/

# 개인 대화 로그 — GitHub 에 올리지 않음
대화기록.txt

# ===== 로컬 전용 문서 / 도구 설정 =====
/.claude/
/docs/screenshots/
/ARCHITECTURE.md
/BACKEND_CLEAN_CODE_REFACTOR.md
/CODEBASE_ANATOMY.md
/PROJECT_CHANGELOG.md
/README.md
/README_OLD.md
/SYNOLOGY_MIGRATION_GUIDE.md
/architecture.svg
/icon.svg

# 브라우저 자동화 도구가 남기는 페이지 덤프 — 저장소에 둘 이유가 없다.
.playwright-mcp/

# 복구 코드·비밀키를 손으로 받아 적어 두는 파일. 평문이라 절대 올라가면 안 된다.
/recovery-codes.txt
/*복구코드*.txt

# 계절 배경 영상의 원본. 저장소 루트에 받아 둔 4K 소스 26개로 2.5GB 다 —
# 배포에 쓰이는 것은 여기서 뽑아낸 popspot-frontend/public/season/*.mp4 (개당 1.5~2MB) 뿐이다.
# 원본은 다시 인코딩할 때만 필요하므로 로컬에 두고 올리지 않는다.
/*.mp4

# 도구가 만드는 작업용 폴더. 산출물이 아니라 중간 파일이라 저장소에 둘 이유가 없다.
/.codex-tmp/
/tmp/
